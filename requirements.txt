python-telegram-bot[job-queue]==21.6
python-dotenv==1.0.1
Pillow==10.4.0

# Для локальной бесплатной транскрипции (TRANSCRIBE_MODE=local, по умолчанию)
faster-whisper==1.0.3

# Для транскрипции через OpenAI Whisper API (TRANSCRIBE_MODE=openai) —
# можно закомментировать, если используешь только local
openai==1.51.0
